# ═══════════════════════════════════════════════════════════════════
#  ResumeIQ — Python Backend Dependencies
#  Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════════

# --- Core Web Framework ---
flask==3.1.1
flask-cors==5.0.1

# --- Environment Variables ---
python-dotenv==1.1.0

# --- Document Extraction ---
PyPDF2==3.0.1            # PDF text extraction
python-docx==1.1.2       # .docx text extraction

# --- NLP (from-scratch + NLTK for future use) ---
nltk==3.9.1              # Tokenizers, stemmers, stopwords (future ML pipeline)

# --- Machine Learning (future use) ---
scikit-learn==1.6.1       # TF-IDF vectorizer, cosine similarity, classifiers
numpy==2.2.5              # Numerical computing
pandas==2.2.3             # Data manipulation

# --- AI Chat ---
groq==0.25.0              # Groq API SDK (Llama 3.3 70B — free tier)

# --- Utilities ---
werkzeug==3.1.3           # WSGI utilities (Flask dependency)
